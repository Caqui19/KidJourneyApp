# KidJourneyApp

# Entrega 1:

- Rode no terminal os seguintes comandos: cd KidJourney (abre a pasta do projeto) e npx expo start --tunnel (rodar projeto);
- Escaneie o QR Code que aparecerá no terminal para abrir o app pelo celular.

* PS: Nathane, não conseguimos finalizar a autenticação a tempo, tendo em vista que decidimos alterar o banco de dados na última hora; em compensação, fizemos boa parte do frontend inicial, preparando-o para o recebimento do backend.